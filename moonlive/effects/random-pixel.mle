// One random light in a random color per frame. The shipped default: always visibly alive,

class RandomPixelEffect {
  int dimensions() { return 1; }

  string tags() { return "💫"; }

  void tick() {
    setRGB(random16(256), random16(256), random16(256), random16(256));
  }
}
