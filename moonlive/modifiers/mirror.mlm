// Mirror along x. Reflecting around `width` (not a fixed 255) keeps every light in the grid.

class MirrorModifier {
  int dimensions() { return 2; }

  string tags() { return "💫"; }

  void modifyLogical() {
    setXYZ(width - 1 - xPos, yPos, zPos);
  }
}
